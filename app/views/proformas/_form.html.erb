<div class="form-row">
	<div class="form-group col-md-6">
		<label for="nombre">Código</label>
	   	<%= f.text_field(:codigo, class: 'form-control', :required => true) %>
	</div>
</div>
<div class="form-row">
	<div class="form-group col-md-6">
		<label for="nombre">Cliente</label>
		
	   	<%= select("post", "clientes_id", Cliente.all.collect {|p| [ p.nombre, p.id ] }, {include_blank: 'None'}) %>
	</div>
	<div class="form-group col-md-6">
	  	<label for="nombre">Proveedor</label>
	   	<%= f.collection_select :post, :supplier_id, Supplier.all, :id, :name, {:class => 'form-control'} %>
	</div>
</div>


